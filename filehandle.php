<?php
    //$file = fopen("text.txt", "r+") or die("Error opening file");
    // echo fread($file, 100); //read 100 byte from file
    // echo fgets($file); //read 1 line from file

    // while(!feof($file)){
    //     echo fgets($file);
    //     if(feof($file)){
    //         echo "\n"."end of file";
    //         break;
    //     }
    // }
    // fclose($file);

    // while(!feof($file)){
    //     echo fgetc($file);
    // }
    // fclose($file);


    // while(($char = fgetc($file)) !== false){
    //     echo $char;
    // }
    // fclose($file);
    

        // $file = fopen("text.txt", "w") or die("Error opening file");
        // $text = "hello world!\n1234567890\n";
        // fwrite($file, $text);
        // $text = "Japanese\n";
        // fwrite($file, $text);
        // fclose($file);

        // $file = fopen("text.txt", 'a');
        // $text = "PHP";
        // fwrite($file, $text);
        // fclose($file);
?>